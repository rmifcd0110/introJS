//Los objetos son una parte principal en JS
//en lugar de crear diferentes variables como estas

let nombreProducto = "Monitor de 20 pulgadas";
let precio = 200;
let disponible = true;

//Podemos crear un objeto que agrupe toda la información
const producto ={
    nombre: "Monitor de 20 pulgadas",
    precio: 200,
    disponible: true //el último no lleva coma
}

console.log(producto.nombre);
