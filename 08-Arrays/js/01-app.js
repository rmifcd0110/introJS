//supongamos que creamos un carrito de compras desde la consola
//más adelante lo haremos en una interfaz

const meses = new Array('Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio');
console.log(meses);

const numeros = [10,20,30,40,50,60];
console.log(numeros);

const deTodo = ['Hola', 10, 'Martin', 'si', null, Date(), {nombre:'Rafael', apellido:'Martín'}, [0,1,2,3,4,5]];
console.log(deTodo);

//un array puede contener otro array
// recuerda que un array es una forma de agrupar grandes cantidades de información en una sola variable

